<%# add navigation links to this file %>
<ul class="nav navbar-nav">
	<% if user_signed_in? %>
	  <li class=""><%= link_to '注销', destroy_user_session_path, :method=>'delete' %></li>
	<% else %>
	  <li class=""><%= link_to '登录', new_user_session_path %></li>
	 <!-- <li class=""><%= link_to 'Sign up', new_user_registration_path %></li> -->
	<% end %>
	<% if user_signed_in? %>
	  <% if current_user.admin?%>
	  	<li class=""><%= link_to '用户', users_path %></li>
	  <% end %>
	<% end %>
</ul>
<ul class="nav navbar-nav navbar-right">
	<% if user_signed_in? %>
		<li class="dropdown">
			<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">其他 <span class="caret"></a>
			<ul class="dropdown-menu">
				<li>
					<%= link_to "博客", blogs_path %>
					<%= link_to "个人资料", edit_user_registration_path %>					
				</li>
			</ul>
		</li>
	<% end %>
</ul>